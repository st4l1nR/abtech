overwrite: true
schema: "http://localhost:3000/api/graphql"
documents: null
generates:
  src/types/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
      - "typescript-mongodb"
